A dark editorial dashboard filling the frame, built as eight full-width horizontal rows stacked from top to bottom on a near-black surface with generous space between them. Each row carries a short name set large in a confident sans-serif at the left and a thin flat activity line at the right. Four of the rows have a bright amber bar running down their extreme left edge, and the other four have a deep grey edge and a faint flat line, so the amber bars read as a single broken column of colour down the left side of the frame. There is no grid of cards anywhere. Modern print-design digital interface, flat vector rendering, restrained palette of near-black, warm grey and a single amber accent, even soft light.

--- wrapper handed to /Users/iuriipoliantsev/.grok/bin/grok ---
Produce exactly one image file and stop.

Step 1. Call image_gen exactly once. Pass aspect_ratio "16:9". Pass this prompt text verbatim, with no additions, no rewording, and no style notes of your own:
<<<PROMPT
A dark editorial dashboard filling the frame, built as eight full-width horizontal rows stacked from top to bottom on a near-black surface with generous space between them. Each row carries a short name set large in a confident sans-serif at the left and a thin flat activity line at the right. Four of the rows have a bright amber bar running down their extreme left edge, and the other four have a deep grey edge and a faint flat line, so the amber bars read as a single broken column of colour down the left side of the frame. There is no grid of cards anywhere. Modern print-design digital interface, flat vector rendering, restrained palette of near-black, warm grey and a single amber accent, even soft light.
PROMPT

Step 2. Copy the resulting image to exactly this absolute path, creating parent directories if they are missing: /Users/iuriipoliantsev/Documents/projects/lorien-worktrees/05a-asset-lever/docs/images/concepts/mission-control/06.jpg

Step 3. Print that absolute path as your entire final message. No prose, no markdown, no code fences, no trailing text.

Hard rules. Exactly one image call, and no second attempt even if the result looks wrong. Do not call image_edit if you called image_gen, or image_gen if you called image_edit. Do not write, move, or delete any file outside /Users/iuriipoliantsev/Documents/projects/lorien-worktrees/05a-asset-lever/docs/images/concepts/mission-control. Do not run git. Do not ask questions.
